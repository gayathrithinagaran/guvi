//4.Creates a slice of array with n elements dropped from the beginning//


function drop(array)
{
    var slicearr=array.slice(1,3);
    console.log(slicearr); //returns slice of array
}
drop([1, 2, 3]) 
